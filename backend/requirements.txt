# ===========================================
# MiroFish Backend Dependencies
# ===========================================
# Python 3.11+ required
# Install: pip install -r requirements.txt
# ===========================================

# ============= Core Framework =============
flask>=3.0.0
flask-cors>=6.0.0

# ============= LLM Related =============
# Claude Agent SDK (uses Claude subscription, no API key needed)
claude-agent-sdk
# OpenAI SDK (still needed for OASIS/camel-ai simulation framework)
openai>=1.0.0

# ============= Zep Cloud =============
zep-cloud==3.13.0

# ============= OASIS Social Media Simulation =============
# OASIS social simulation framework (requires Python <3.12, using Python 3.11 venv)
camel-oasis==0.2.5
camel-ai==0.2.78

# ============= File Processing =============
PyMuPDF>=1.24.0
# Encoding detection (supports text files with non-UTF-8 encodings)
charset-normalizer>=3.0.0
chardet>=5.0.0

# ============= Utility Libraries =============
# Environment variable loading
python-dotenv>=1.0.0

# Data validation
pydantic>=2.0.0
